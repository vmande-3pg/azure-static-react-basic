{
    "routes": [
        {
            "route": "/profile",
            "allowedRoles": ["authenticated"]
        },
        {
            "route": "/login",
            "rewrite": "/.auth/login/ad"
        }
    ],
    "navigationFallback": {
        "rewrite": "/index.html"
    }
}